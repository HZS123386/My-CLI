{
  "name": "resume-cli-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "AI resume PDF parsing and JD matching CLI demo",
  "bin": {
    "resume-cli": "dist/src/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "resume-cli": "tsx src/cli.ts",
    "generate:example": "tsx scripts/create-example-pdf.ts",
    "demo": "tsx scripts/create-example-pdf.ts && tsx src/cli.ts parse examples/resume-sample.pdf && tsx src/cli.ts extract examples/resume-sample.pdf --mock && tsx src/cli.ts score examples/resume-sample.pdf --jd examples/jd-sample.txt --mock",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "commander": "^14.0.0",
    "dotenv": "^17.2.1",
    "pdf-parse": "^2.4.5"
  },
  "devDependencies": {
    "@types/node": "^24.3.0",
    "pdf-lib": "^1.17.1",
    "tsx": "^4.20.5",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4"
  },
  "engines": {
    "node": ">=20"
  }
}
